# Copy this file to secrets.toml (gitignored) and fill in a real key to
# enable the AI Summary tab's live API calls. Without it, that tab shows
# the assembled prompt for manual copy-paste instead.
# Get a key from Google AI Studio: https://aistudio.google.com/apikey
GOOGLE_API_KEY = "AIza..."
